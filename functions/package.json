{
  "name": "functions",
  "version": "1.0.0",
  "description": "Test suite that exercises the real Supabase Edge Functions (supabase/functions/) and the live Postgres RLS policies. Contains no runtime code of its own — the src/ implementation was a Firebase-era duplicate abandoned when the project migrated to Supabase, and was deleted after it was found to have silently diverged from production.",
  "scripts": {
    "test": "vitest run tests/signature.test.ts tests/stateMachine.test.ts tests/rls.test.ts",
    "test:watch": "vitest",
    "test:rls": "vitest run tests/rls.test.ts",
    "test:rules": "vitest run tests/rls.test.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.116.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.9",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  },
  "private": true
}
